spring:
  application:
    name: config-server
  config:
     name: config-server
  cloud:
    config:
      server:
        git:
         uri: https://github.com/payalbnsl/ConfigServerRep
eureka:
  client:
    registerWithEureka: true    
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8086/eureka/
server:
  port: 8888

  
